import React from 'react';
import { motion } from 'framer-motion';

export default function StatCard({ title, value, change, icon: Icon, index }) {
  const isPositive = change >= 0;

  return null;




















}
